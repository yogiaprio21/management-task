# UI Components

This directory is reserved for reusable UI components (e.g., buttons, inputs, modals).
Currently, components are located in `src/components`.
Recommended: Move shared components like `Spinner` here or use this for a component library like shadcn/ui.
