# Custom Hooks

This directory is reserved for custom React hooks.
Currently, context hooks like `useAuth` and `useSocket` are in `src/context`.
Recommended: Place general purpose hooks (e.g., `useLocalStorage`, `useDebounce`) here.
